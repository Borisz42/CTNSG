# ATLAS Evidence Composition

This directory tracks generation provenance.

## Purpose
It cryptographically binds the Layer-1 syntactic generation traces with the Layer-2 logical proofs into verifiable evidence bundles. This ensures full auditability of the generated output.
